// Generated by dust2 (version 0.3.23) - do not edit

// Generated by odin2 (version 0.3.26) - do not edit
#include <dust2/common.hpp>
// [[dust2::class(basic)]]
// [[dust2::time_type(discrete)]]
// [[dust2::has_compare()]]
// [[dust2::parameter(n_age_groups, type = "int", rank = 0, required = TRUE, constant = TRUE)]]
// [[dust2::parameter(n_trans_classes, type = "int", rank = 0, required = FALSE, constant = TRUE)]]
// [[dust2::parameter(seed_time, type = "real_type", rank = 1, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(seed_value, type = "real_type", rank = 1, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(k_E, type = "int", rank = 0, required = TRUE, constant = TRUE)]]
// [[dust2::parameter(gamma_E, type = "real_type", rank = 0, required = FALSE, constant = FALSE)]]
// [[dust2::parameter(p_C, type = "real_type", rank = 1, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(k_A, type = "int", rank = 0, required = TRUE, constant = TRUE)]]
// [[dust2::parameter(gamma_A, type = "real_type", rank = 0, required = FALSE, constant = FALSE)]]
// [[dust2::parameter(k_C, type = "int", rank = 0, required = TRUE, constant = TRUE)]]
// [[dust2::parameter(gamma_C, type = "real_type", rank = 0, required = FALSE, constant = FALSE)]]
// [[dust2::parameter(p_recov_sympt, type = "real_type", rank = 1, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(k_hosp, type = "int", rank = 0, required = TRUE, constant = TRUE)]]
// [[dust2::parameter(gamma_hosp, type = "real_type", rank = 0, required = FALSE, constant = FALSE)]]
// [[dust2::parameter(p_recov_hosp, type = "real_type", rank = 1, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(p_death_hosp, type = "real_type", rank = 1, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(k_ICU, type = "int", rank = 0, required = TRUE, constant = TRUE)]]
// [[dust2::parameter(gamma_ICU, type = "real_type", rank = 0, required = FALSE, constant = FALSE)]]
// [[dust2::parameter(p_recov_ICU, type = "real_type", rank = 1, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(k_rec, type = "int", rank = 0, required = TRUE, constant = TRUE)]]
// [[dust2::parameter(gamma_rec, type = "real_type", rank = 0, required = FALSE, constant = FALSE)]]
// [[dust2::parameter(beta_time, type = "real_type", rank = 1, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(beta_value, type = "real_type", rank = 1, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(m, type = "real_type", rank = 2, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(hosp_transmission, type = "real_type", rank = 0, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(ICU_transmission, type = "real_type", rank = 0, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(exp_noise, type = "real_type", rank = 0, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(phi_ICU, type = "real_type", rank = 0, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(kappa_ICU, type = "real_type", rank = 0, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(phi_death, type = "real_type", rank = 0, required = TRUE, constant = FALSE)]]
// [[dust2::parameter(kappa_death, type = "real_type", rank = 0, required = TRUE, constant = FALSE)]]
class basic {
public:
  basic() = delete;
  using real_type = double;
  using rng_state_type = monty::random::generator<real_type>;
  struct shared_state {
    struct odin_internals_type {
      struct {
        dust2::packing state;
      } packing;
      struct {
        std::array<size_t, 14> state;
      } offset;
    } odin;
    struct dim_type {
      dust2::array::dimensions<1> seed_time;
      dust2::array::dimensions<1> seed_value;
      dust2::array::dimensions<1> beta_time;
      dust2::array::dimensions<1> beta_value;
      dust2::array::dimensions<1> S;
      dust2::array::dimensions<3> E;
      dust2::array::dimensions<3> aux_EE;
      dust2::array::dimensions<3> delta_E;
      dust2::array::dimensions<3> n_EE;
      dust2::array::dimensions<3> I_A;
      dust2::array::dimensions<3> aux_II_A;
      dust2::array::dimensions<3> delta_I_A;
      dust2::array::dimensions<3> n_II_A;
      dust2::array::dimensions<3> I_C;
      dust2::array::dimensions<3> aux_II_C;
      dust2::array::dimensions<3> delta_I_C;
      dust2::array::dimensions<3> n_II_C;
      dust2::array::dimensions<1> p_recov_sympt;
      dust2::array::dimensions<3> I_hosp;
      dust2::array::dimensions<3> aux_II_hosp;
      dust2::array::dimensions<3> delta_I_hosp;
      dust2::array::dimensions<3> n_II_hosp;
      dust2::array::dimensions<1> p_recov_hosp;
      dust2::array::dimensions<2> n_death_hosp;
      dust2::array::dimensions<3> I_ICU;
      dust2::array::dimensions<3> aux_II_ICU;
      dust2::array::dimensions<3> delta_I_ICU;
      dust2::array::dimensions<3> n_II_ICU;
      dust2::array::dimensions<1> p_recov_ICU;
      dust2::array::dimensions<3> new_I_ICU;
      dust2::array::dimensions<3> R_hosp;
      dust2::array::dimensions<3> aux_R_hosp;
      dust2::array::dimensions<3> delta_R_hosp;
      dust2::array::dimensions<3> n_R_hosp;
      dust2::array::dimensions<1> R;
      dust2::array::dimensions<1> delta_R;
      dust2::array::dimensions<1> D;
      dust2::array::dimensions<1> delta_D;
      dust2::array::dimensions<1> new_D;
      dust2::array::dimensions<1> p_SE;
      dust2::array::dimensions<1> n_SE;
      dust2::array::dimensions<2> aux_p_bin;
      dust2::array::dimensions<2> n_EI_A;
      dust2::array::dimensions<2> n_EI_C;
      dust2::array::dimensions<2> n_hosp_to_ICU;
      dust2::array::dimensions<2> n_sympt_to_hosp;
      dust2::array::dimensions<2> n_ICU_to_R_hosp;
      dust2::array::dimensions<1> p_C;
      dust2::array::dimensions<1> p_death_hosp;
      dust2::array::dimensions<1> lambda;
      dust2::array::dimensions<2> s_ij;
      dust2::array::dimensions<2> m;
      dust2::array::dimensions<2> trans_profile;
      dust2::array::dimensions<2> trans_increase;
      dust2::array::dimensions<2> I_with_diff_trans;
    } dim;
    int n_age_groups;
    int n_trans_classes;
    int seed_age_band;
    int k_E;
    real_type gamma_E;
    int k_A;
    real_type gamma_A;
    int k_C;
    real_type gamma_C;
    int k_hosp;
    real_type gamma_hosp;
    int k_ICU;
    real_type gamma_ICU;
    int k_rec;
    real_type gamma_rec;
    real_type hosp_transmission;
    real_type ICU_transmission;
    real_type exp_noise;
    real_type phi_ICU;
    real_type kappa_ICU;
    real_type phi_death;
    real_type kappa_death;
    std::vector<real_type> seed_time;
    std::vector<real_type> seed_value;
    std::vector<real_type> beta_time;
    std::vector<real_type> beta_value;
    dust2::interpolate::InterpolateConstant<real_type> interpolate_seed;
    std::vector<real_type> p_C;
    std::vector<real_type> p_recov_sympt;
    std::vector<real_type> p_recov_hosp;
    std::vector<real_type> p_death_hosp;
    std::vector<real_type> p_recov_ICU;
    dust2::interpolate::InterpolateConstant<real_type> interpolate_beta;
    std::vector<real_type> m;
    std::vector<real_type> trans_profile;
    std::vector<real_type> trans_increase;
    std::vector<real_type> aux_p_bin;
  };
  struct internal_state {
    std::vector<real_type> n_EE;
    std::vector<real_type> n_II_A;
    std::vector<real_type> n_II_C;
    std::vector<real_type> n_II_hosp;
    std::vector<real_type> n_II_ICU;
    std::vector<real_type> n_R_hosp;
    std::vector<real_type> n_EI_A;
    std::vector<real_type> n_sympt_to_hosp;
    std::vector<real_type> n_death_hosp;
    std::vector<real_type> n_ICU_to_R_hosp;
    std::vector<real_type> I_with_diff_trans;
    std::vector<real_type> n_EI_C;
    std::vector<real_type> aux_II_A;
    std::vector<real_type> aux_II_hosp;
    std::vector<real_type> n_hosp_to_ICU;
    std::vector<real_type> aux_R_hosp;
    std::vector<real_type> delta_D;
    std::vector<real_type> s_ij;
    std::vector<real_type> delta_I_A;
    std::vector<real_type> aux_II_C;
    std::vector<real_type> delta_I_hosp;
    std::vector<real_type> aux_II_ICU;
    std::vector<real_type> delta_R_hosp;
    std::vector<real_type> new_D;
    std::vector<real_type> delta_R;
    std::vector<real_type> lambda;
    std::vector<real_type> p_SE;
    std::vector<real_type> delta_I_C;
    std::vector<real_type> delta_I_ICU;
    std::vector<real_type> n_SE;
    std::vector<real_type> new_I_ICU;
    std::vector<real_type> aux_EE;
    std::vector<real_type> delta_E;
  };
  struct data_type {
    real_type icu;
    real_type deaths;
  };
  static dust2::packing packing_state(const shared_state& shared) {
    return shared.odin.packing.state;
  }
  static shared_state build_shared(cpp11::list parameters) {
    shared_state::dim_type dim;
    const int n_age_groups = dust2::r::read_int(parameters, "n_age_groups");
    const int n_trans_classes = dust2::r::read_int(parameters, "n_trans_classes", 1);
    const int seed_age_band = static_cast<int>(4);
    dim.seed_time = dust2::r::read_dimensions<1>(parameters, "seed_time");
    dim.seed_value = dust2::r::read_dimensions<1>(parameters, "seed_value");
    const int k_E = dust2::r::read_int(parameters, "k_E");
    const real_type gamma_E = dust2::r::read_real(parameters, "gamma_E", static_cast<real_type>(0.10000000000000001));
    const int k_A = dust2::r::read_int(parameters, "k_A");
    const real_type gamma_A = dust2::r::read_real(parameters, "gamma_A", static_cast<real_type>(0.10000000000000001));
    const int k_C = dust2::r::read_int(parameters, "k_C");
    const real_type gamma_C = dust2::r::read_real(parameters, "gamma_C", static_cast<real_type>(0.10000000000000001));
    const int k_hosp = dust2::r::read_int(parameters, "k_hosp");
    const real_type gamma_hosp = dust2::r::read_real(parameters, "gamma_hosp", static_cast<real_type>(0.10000000000000001));
    const int k_ICU = dust2::r::read_int(parameters, "k_ICU");
    const real_type gamma_ICU = dust2::r::read_real(parameters, "gamma_ICU", static_cast<real_type>(0.10000000000000001));
    const int k_rec = dust2::r::read_int(parameters, "k_rec");
    const real_type gamma_rec = dust2::r::read_real(parameters, "gamma_rec", static_cast<real_type>(0.10000000000000001));
    dim.beta_time = dust2::r::read_dimensions<1>(parameters, "beta_time");
    dim.beta_value = dust2::r::read_dimensions<1>(parameters, "beta_value");
    const real_type hosp_transmission = dust2::r::read_real(parameters, "hosp_transmission");
    const real_type ICU_transmission = dust2::r::read_real(parameters, "ICU_transmission");
    const real_type exp_noise = dust2::r::read_real(parameters, "exp_noise");
    const real_type phi_ICU = dust2::r::read_real(parameters, "phi_ICU");
    const real_type kappa_ICU = dust2::r::read_real(parameters, "kappa_ICU");
    const real_type phi_death = dust2::r::read_real(parameters, "phi_death");
    const real_type kappa_death = dust2::r::read_real(parameters, "kappa_death");
    std::vector<real_type> seed_time(dim.seed_time.size);
    dust2::r::read_real_array(parameters, dim.seed_time, seed_time.data(), "seed_time", true);
    std::vector<real_type> seed_value(dim.seed_value.size);
    dust2::r::read_real_array(parameters, dim.seed_value, seed_value.data(), "seed_value", true);
    std::vector<real_type> beta_time(dim.beta_time.size);
    dust2::r::read_real_array(parameters, dim.beta_time, beta_time.data(), "beta_time", true);
    std::vector<real_type> beta_value(dim.beta_value.size);
    dust2::r::read_real_array(parameters, dim.beta_value, beta_value.data(), "beta_value", true);
    dim.S.set({static_cast<size_t>(n_age_groups)});
    dim.E.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_E), static_cast<size_t>(n_trans_classes)});
    dim.aux_EE.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_E), static_cast<size_t>(n_trans_classes)});
    dim.delta_E.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_E), static_cast<size_t>(n_trans_classes)});
    dim.n_EE.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_E), static_cast<size_t>(n_trans_classes)});
    dim.I_A.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_A), static_cast<size_t>(n_trans_classes)});
    dim.aux_II_A.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_A), static_cast<size_t>(n_trans_classes)});
    dim.delta_I_A.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_A), static_cast<size_t>(n_trans_classes)});
    dim.n_II_A.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_A), static_cast<size_t>(n_trans_classes)});
    dim.I_C.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_C), static_cast<size_t>(n_trans_classes)});
    dim.aux_II_C.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_C), static_cast<size_t>(n_trans_classes)});
    dim.delta_I_C.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_C), static_cast<size_t>(n_trans_classes)});
    dim.n_II_C.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_C), static_cast<size_t>(n_trans_classes)});
    dim.p_recov_sympt.set({static_cast<size_t>(n_age_groups)});
    dim.I_hosp.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_hosp), static_cast<size_t>(n_trans_classes)});
    dim.aux_II_hosp.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_hosp), static_cast<size_t>(n_trans_classes)});
    dim.delta_I_hosp.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_hosp), static_cast<size_t>(n_trans_classes)});
    dim.n_II_hosp.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_hosp), static_cast<size_t>(n_trans_classes)});
    dim.p_recov_hosp.set({static_cast<size_t>(n_age_groups)});
    dim.n_death_hosp.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(n_trans_classes)});
    dim.I_ICU.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_ICU), static_cast<size_t>(n_trans_classes)});
    dim.aux_II_ICU.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_ICU), static_cast<size_t>(n_trans_classes)});
    dim.delta_I_ICU.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_ICU), static_cast<size_t>(n_trans_classes)});
    dim.n_II_ICU.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_ICU), static_cast<size_t>(n_trans_classes)});
    dim.p_recov_ICU.set({static_cast<size_t>(n_age_groups)});
    dim.new_I_ICU.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_ICU), static_cast<size_t>(n_trans_classes)});
    dim.R_hosp.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_rec), static_cast<size_t>(n_trans_classes)});
    dim.aux_R_hosp.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_rec), static_cast<size_t>(n_trans_classes)});
    dim.delta_R_hosp.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_rec), static_cast<size_t>(n_trans_classes)});
    dim.n_R_hosp.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(k_rec), static_cast<size_t>(n_trans_classes)});
    dim.R.set({static_cast<size_t>(n_age_groups)});
    dim.delta_R.set({static_cast<size_t>(n_age_groups)});
    dim.D.set({static_cast<size_t>(n_age_groups)});
    dim.delta_D.set({static_cast<size_t>(n_age_groups)});
    dim.new_D.set({static_cast<size_t>(n_age_groups)});
    dim.p_SE.set({static_cast<size_t>(n_age_groups)});
    dim.n_SE.set({static_cast<size_t>(n_age_groups)});
    dim.aux_p_bin.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(n_trans_classes)});
    dim.n_EI_A.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(n_trans_classes)});
    dim.n_EI_C.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(n_trans_classes)});
    dim.n_hosp_to_ICU.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(n_trans_classes)});
    dim.n_sympt_to_hosp.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(n_trans_classes)});
    dim.n_ICU_to_R_hosp.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(n_trans_classes)});
    dim.p_C.set({static_cast<size_t>(n_age_groups)});
    dim.p_death_hosp.set({static_cast<size_t>(n_age_groups)});
    dim.lambda.set({static_cast<size_t>(n_age_groups)});
    dim.s_ij.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(n_age_groups)});
    dim.m.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(n_age_groups)});
    dim.trans_profile.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(n_trans_classes)});
    dim.trans_increase.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(n_trans_classes)});
    dim.I_with_diff_trans.set({static_cast<size_t>(n_age_groups), static_cast<size_t>(n_trans_classes)});
    const auto interpolate_seed = dust2::interpolate::InterpolateConstant(seed_time, seed_value, "seed_time", "seed_value");
    std::vector<real_type> p_C(dim.p_C.size);
    dust2::r::read_real_array(parameters, dim.p_C, p_C.data(), "p_C", true);
    std::vector<real_type> p_recov_sympt(dim.p_recov_sympt.size);
    dust2::r::read_real_array(parameters, dim.p_recov_sympt, p_recov_sympt.data(), "p_recov_sympt", true);
    std::vector<real_type> p_recov_hosp(dim.p_recov_hosp.size);
    dust2::r::read_real_array(parameters, dim.p_recov_hosp, p_recov_hosp.data(), "p_recov_hosp", true);
    std::vector<real_type> p_death_hosp(dim.p_death_hosp.size);
    dust2::r::read_real_array(parameters, dim.p_death_hosp, p_death_hosp.data(), "p_death_hosp", true);
    std::vector<real_type> p_recov_ICU(dim.p_recov_ICU.size);
    dust2::r::read_real_array(parameters, dim.p_recov_ICU, p_recov_ICU.data(), "p_recov_ICU", true);
    const auto interpolate_beta = dust2::interpolate::InterpolateConstant(beta_time, beta_value, "beta_time", "beta_value");
    std::vector<real_type> m(dim.m.size);
    dust2::r::read_real_array(parameters, dim.m, m.data(), "m", true);
    std::vector<real_type> trans_profile(dim.trans_profile.size);
    for (size_t i = 1; i <= dim.trans_profile.dim[0]; ++i) {
      for (size_t j = 1; j <= dim.trans_profile.dim[1]; ++j) {
        trans_profile[i - 1 + (j - 1) * dim.trans_profile.mult[1]] = 1;
      }
    }
    std::vector<real_type> trans_increase(dim.trans_increase.size);
    for (size_t i = 1; i <= dim.trans_increase.dim[0]; ++i) {
      for (size_t j = 1; j <= dim.trans_increase.dim[1]; ++j) {
        trans_increase[i - 1 + (j - 1) * dim.trans_increase.mult[1]] = 1;
      }
    }
    std::vector<real_type> aux_p_bin(dim.aux_p_bin.size);
    for (size_t i = 1; i <= dim.aux_p_bin.dim[0]; ++i) {
      aux_p_bin[i - 1] = trans_profile[i - 1];
    }
    for (size_t i = 1; i <= dim.aux_p_bin.dim[0]; ++i) {
      for (size_t j = 2; j <= static_cast<size_t>((n_trans_classes - 1)); ++j) {
        aux_p_bin[i - 1 + (j - 1) * dim.aux_p_bin.mult[1]] = trans_profile[i - 1 + (j - 1) * dim.trans_profile.mult[1]] / dust2::array::sum<real_type>(trans_profile.data(), dim.trans_profile, {i - 1, i - 1}, {j - 1, n_trans_classes - 1});
      }
    }
    shared_state::odin_internals_type odin;
    odin.packing.state = dust2::packing{
      {"N_tot", {}},
      {"I_ICU_tot", {}},
      {"D_tot", {}},
      {"D_inc", {}},
      {"beta_out", {}},
      {"S", std::vector<size_t>(dim.S.dim.begin(), dim.S.dim.end())},
      {"E", std::vector<size_t>(dim.E.dim.begin(), dim.E.dim.end())},
      {"I_A", std::vector<size_t>(dim.I_A.dim.begin(), dim.I_A.dim.end())},
      {"I_C", std::vector<size_t>(dim.I_C.dim.begin(), dim.I_C.dim.end())},
      {"I_hosp", std::vector<size_t>(dim.I_hosp.dim.begin(), dim.I_hosp.dim.end())},
      {"I_ICU", std::vector<size_t>(dim.I_ICU.dim.begin(), dim.I_ICU.dim.end())},
      {"R_hosp", std::vector<size_t>(dim.R_hosp.dim.begin(), dim.R_hosp.dim.end())},
      {"R", std::vector<size_t>(dim.R.dim.begin(), dim.R.dim.end())},
      {"D", std::vector<size_t>(dim.D.dim.begin(), dim.D.dim.end())}
    };
    odin.packing.state.copy_offset(odin.offset.state.begin());
    return shared_state{odin, dim, n_age_groups, n_trans_classes, seed_age_band, k_E, gamma_E, k_A, gamma_A, k_C, gamma_C, k_hosp, gamma_hosp, k_ICU, gamma_ICU, k_rec, gamma_rec, hosp_transmission, ICU_transmission, exp_noise, phi_ICU, kappa_ICU, phi_death, kappa_death, seed_time, seed_value, beta_time, beta_value, interpolate_seed, p_C, p_recov_sympt, p_recov_hosp, p_death_hosp, p_recov_ICU, interpolate_beta, m, trans_profile, trans_increase, aux_p_bin};
  }
  static internal_state build_internal(const shared_state& shared) {
    std::vector<real_type> n_EE(shared.dim.n_EE.size);
    std::vector<real_type> n_II_A(shared.dim.n_II_A.size);
    std::vector<real_type> n_II_C(shared.dim.n_II_C.size);
    std::vector<real_type> n_II_hosp(shared.dim.n_II_hosp.size);
    std::vector<real_type> n_II_ICU(shared.dim.n_II_ICU.size);
    std::vector<real_type> n_R_hosp(shared.dim.n_R_hosp.size);
    std::vector<real_type> n_EI_A(shared.dim.n_EI_A.size);
    std::vector<real_type> n_sympt_to_hosp(shared.dim.n_sympt_to_hosp.size);
    std::vector<real_type> n_death_hosp(shared.dim.n_death_hosp.size);
    std::vector<real_type> n_ICU_to_R_hosp(shared.dim.n_ICU_to_R_hosp.size);
    std::vector<real_type> I_with_diff_trans(shared.dim.I_with_diff_trans.size);
    std::vector<real_type> n_EI_C(shared.dim.n_EI_C.size);
    std::vector<real_type> aux_II_A(shared.dim.aux_II_A.size);
    std::vector<real_type> aux_II_hosp(shared.dim.aux_II_hosp.size);
    std::vector<real_type> n_hosp_to_ICU(shared.dim.n_hosp_to_ICU.size);
    std::vector<real_type> aux_R_hosp(shared.dim.aux_R_hosp.size);
    std::vector<real_type> delta_D(shared.dim.delta_D.size);
    std::vector<real_type> s_ij(shared.dim.s_ij.size);
    std::vector<real_type> delta_I_A(shared.dim.delta_I_A.size);
    std::vector<real_type> aux_II_C(shared.dim.aux_II_C.size);
    std::vector<real_type> delta_I_hosp(shared.dim.delta_I_hosp.size);
    std::vector<real_type> aux_II_ICU(shared.dim.aux_II_ICU.size);
    std::vector<real_type> delta_R_hosp(shared.dim.delta_R_hosp.size);
    std::vector<real_type> new_D(shared.dim.new_D.size);
    std::vector<real_type> delta_R(shared.dim.delta_R.size);
    std::vector<real_type> lambda(shared.dim.lambda.size);
    std::vector<real_type> p_SE(shared.dim.p_SE.size);
    std::vector<real_type> delta_I_C(shared.dim.delta_I_C.size);
    std::vector<real_type> delta_I_ICU(shared.dim.delta_I_ICU.size);
    std::vector<real_type> n_SE(shared.dim.n_SE.size);
    std::vector<real_type> new_I_ICU(shared.dim.new_I_ICU.size);
    std::vector<real_type> aux_EE(shared.dim.aux_EE.size);
    std::vector<real_type> delta_E(shared.dim.delta_E.size);
    return internal_state{n_EE, n_II_A, n_II_C, n_II_hosp, n_II_ICU, n_R_hosp, n_EI_A, n_sympt_to_hosp, n_death_hosp, n_ICU_to_R_hosp, I_with_diff_trans, n_EI_C, aux_II_A, aux_II_hosp, n_hosp_to_ICU, aux_R_hosp, delta_D, s_ij, delta_I_A, aux_II_C, delta_I_hosp, aux_II_ICU, delta_R_hosp, new_D, delta_R, lambda, p_SE, delta_I_C, delta_I_ICU, n_SE, new_I_ICU, aux_EE, delta_E};
  }
  static data_type build_data(cpp11::list data, const shared_state& shared) {
    auto icu = dust2::r::read_real(data, "icu", NA_REAL);
    auto deaths = dust2::r::read_real(data, "deaths", NA_REAL);
    return data_type{icu, deaths};
  }
  static void update_shared(cpp11::list parameters, shared_state& shared) {
    shared.gamma_E = dust2::r::read_real(parameters, "gamma_E", shared.gamma_E);
    shared.gamma_A = dust2::r::read_real(parameters, "gamma_A", shared.gamma_A);
    shared.gamma_C = dust2::r::read_real(parameters, "gamma_C", shared.gamma_C);
    shared.gamma_hosp = dust2::r::read_real(parameters, "gamma_hosp", shared.gamma_hosp);
    shared.gamma_ICU = dust2::r::read_real(parameters, "gamma_ICU", shared.gamma_ICU);
    shared.gamma_rec = dust2::r::read_real(parameters, "gamma_rec", shared.gamma_rec);
    shared.hosp_transmission = dust2::r::read_real(parameters, "hosp_transmission", shared.hosp_transmission);
    shared.ICU_transmission = dust2::r::read_real(parameters, "ICU_transmission", shared.ICU_transmission);
    shared.exp_noise = dust2::r::read_real(parameters, "exp_noise", shared.exp_noise);
    shared.phi_ICU = dust2::r::read_real(parameters, "phi_ICU", shared.phi_ICU);
    shared.kappa_ICU = dust2::r::read_real(parameters, "kappa_ICU", shared.kappa_ICU);
    shared.phi_death = dust2::r::read_real(parameters, "phi_death", shared.phi_death);
    shared.kappa_death = dust2::r::read_real(parameters, "kappa_death", shared.kappa_death);
    dust2::r::read_real_array(parameters, shared.dim.seed_time, shared.seed_time.data(), "seed_time", false);
    dust2::r::read_real_array(parameters, shared.dim.seed_value, shared.seed_value.data(), "seed_value", false);
    dust2::r::read_real_array(parameters, shared.dim.beta_time, shared.beta_time.data(), "beta_time", false);
    dust2::r::read_real_array(parameters, shared.dim.beta_value, shared.beta_value.data(), "beta_value", false);
    const auto interpolate_seed = dust2::interpolate::InterpolateConstant(shared.seed_time, shared.seed_value, "seed_time", "seed_value");
    dust2::r::read_real_array(parameters, shared.dim.p_C, shared.p_C.data(), "p_C", false);
    dust2::r::read_real_array(parameters, shared.dim.p_recov_sympt, shared.p_recov_sympt.data(), "p_recov_sympt", false);
    dust2::r::read_real_array(parameters, shared.dim.p_recov_hosp, shared.p_recov_hosp.data(), "p_recov_hosp", false);
    dust2::r::read_real_array(parameters, shared.dim.p_death_hosp, shared.p_death_hosp.data(), "p_death_hosp", false);
    dust2::r::read_real_array(parameters, shared.dim.p_recov_ICU, shared.p_recov_ICU.data(), "p_recov_ICU", false);
    const auto interpolate_beta = dust2::interpolate::InterpolateConstant(shared.beta_time, shared.beta_value, "beta_time", "beta_value");
    dust2::r::read_real_array(parameters, shared.dim.m, shared.m.data(), "m", false);
  }
  static void update_internal(const shared_state& shared, internal_state& internal) {
  }
  static void initial(real_type time, const shared_state& shared, internal_state& internal, rng_state_type& rng_state, real_type* state) {
    for (size_t i = 1; i <= shared.dim.S.size; ++i) {
      state[i - 1 + 5] = 0;
    }
    for (size_t i = 1; i <= shared.dim.E.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.E.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.E.dim[2]; ++k) {
          state[i - 1 + (j - 1) * shared.dim.E.mult[1] + (k - 1) * shared.dim.E.mult[2] + shared.odin.offset.state[6]] = 0;
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.I_A.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.I_A.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.I_A.dim[2]; ++k) {
          state[i - 1 + (j - 1) * shared.dim.I_A.mult[1] + (k - 1) * shared.dim.I_A.mult[2] + shared.odin.offset.state[7]] = 0;
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.I_C.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.I_C.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.I_C.dim[2]; ++k) {
          state[i - 1 + (j - 1) * shared.dim.I_C.mult[1] + (k - 1) * shared.dim.I_C.mult[2] + shared.odin.offset.state[8]] = 0;
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.I_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.I_hosp.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.I_hosp.dim[2]; ++k) {
          state[i - 1 + (j - 1) * shared.dim.I_hosp.mult[1] + (k - 1) * shared.dim.I_hosp.mult[2] + shared.odin.offset.state[9]] = 0;
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.I_ICU.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.I_ICU.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.I_ICU.dim[2]; ++k) {
          state[i - 1 + (j - 1) * shared.dim.I_ICU.mult[1] + (k - 1) * shared.dim.I_ICU.mult[2] + shared.odin.offset.state[10]] = 0;
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.R_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.R_hosp.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.R_hosp.dim[2]; ++k) {
          state[i - 1 + (j - 1) * shared.dim.R_hosp.mult[1] + (k - 1) * shared.dim.R_hosp.mult[2] + shared.odin.offset.state[11]] = 0;
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.R.size; ++i) {
      state[i - 1 + shared.odin.offset.state[12]] = 0;
    }
    for (size_t i = 1; i <= shared.dim.D.size; ++i) {
      state[i - 1 + shared.odin.offset.state[13]] = 0;
    }
    state[0] = 0;
    state[1] = 0;
    state[2] = 0;
    state[3] = 0;
    state[4] = shared.beta_value[0];
  }
  static void update(real_type time, real_type dt, const real_type* state, const shared_state& shared, internal_state& internal, rng_state_type& rng_state, real_type* state_next) {
    const auto * S = state + 5;
    const auto * E = state + shared.odin.offset.state[6];
    const auto * I_A = state + shared.odin.offset.state[7];
    const auto * I_C = state + shared.odin.offset.state[8];
    const auto * I_hosp = state + shared.odin.offset.state[9];
    const auto * I_ICU = state + shared.odin.offset.state[10];
    const auto * R_hosp = state + shared.odin.offset.state[11];
    const auto * R = state + shared.odin.offset.state[12];
    const auto * D = state + shared.odin.offset.state[13];
    const auto D_tot = state[2];
    const auto D_inc = state[3];
    const real_type p_EE = 1 - monty::math::exp(-shared.gamma_E * dt);
    const real_type p_II_A = 1 - monty::math::exp(-shared.gamma_A * dt);
    const real_type p_II_C = 1 - monty::math::exp(-shared.gamma_C * dt);
    const real_type p_II_hosp = 1 - monty::math::exp(-shared.gamma_hosp * dt);
    const real_type p_II_ICU = 1 - monty::math::exp(-shared.gamma_ICU * dt);
    const real_type p_R_hosp = 1 - monty::math::exp(-shared.gamma_rec * dt);
    for (size_t i = 1; i <= shared.dim.n_EE.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.n_EE.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.n_EE.dim[2]; ++k) {
          internal.n_EE[i - 1 + (j - 1) * shared.dim.n_EE.mult[1] + (k - 1) * shared.dim.n_EE.mult[2]] = monty::random::binomial<real_type>(rng_state, E[i - 1 + (j - 1) * shared.dim.E.mult[1] + (k - 1) * shared.dim.E.mult[2]], p_EE);
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.n_II_A.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.n_II_A.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.n_II_A.dim[2]; ++k) {
          internal.n_II_A[i - 1 + (j - 1) * shared.dim.n_II_A.mult[1] + (k - 1) * shared.dim.n_II_A.mult[2]] = monty::random::binomial<real_type>(rng_state, I_A[i - 1 + (j - 1) * shared.dim.I_A.mult[1] + (k - 1) * shared.dim.I_A.mult[2]], p_II_A);
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.n_II_C.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.n_II_C.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.n_II_C.dim[2]; ++k) {
          internal.n_II_C[i - 1 + (j - 1) * shared.dim.n_II_C.mult[1] + (k - 1) * shared.dim.n_II_C.mult[2]] = monty::random::binomial<real_type>(rng_state, I_C[i - 1 + (j - 1) * shared.dim.I_C.mult[1] + (k - 1) * shared.dim.I_C.mult[2]], p_II_C);
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.n_II_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.n_II_hosp.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.n_II_hosp.dim[2]; ++k) {
          internal.n_II_hosp[i - 1 + (j - 1) * shared.dim.n_II_hosp.mult[1] + (k - 1) * shared.dim.n_II_hosp.mult[2]] = monty::random::binomial<real_type>(rng_state, I_hosp[i - 1 + (j - 1) * shared.dim.I_hosp.mult[1] + (k - 1) * shared.dim.I_hosp.mult[2]], p_II_hosp);
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.n_II_ICU.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.n_II_ICU.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.n_II_ICU.dim[2]; ++k) {
          internal.n_II_ICU[i - 1 + (j - 1) * shared.dim.n_II_ICU.mult[1] + (k - 1) * shared.dim.n_II_ICU.mult[2]] = monty::random::binomial<real_type>(rng_state, I_ICU[i - 1 + (j - 1) * shared.dim.I_ICU.mult[1] + (k - 1) * shared.dim.I_ICU.mult[2]], p_II_ICU);
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.n_R_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.n_R_hosp.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.n_R_hosp.dim[2]; ++k) {
          internal.n_R_hosp[i - 1 + (j - 1) * shared.dim.n_R_hosp.mult[1] + (k - 1) * shared.dim.n_R_hosp.mult[2]] = monty::random::binomial<real_type>(rng_state, R_hosp[i - 1 + (j - 1) * shared.dim.R_hosp.mult[1] + (k - 1) * shared.dim.R_hosp.mult[2]], p_R_hosp);
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.n_EI_A.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.n_EI_A.dim[1]; ++j) {
        internal.n_EI_A[i - 1 + (j - 1) * shared.dim.n_EI_A.mult[1]] = monty::random::binomial<real_type>(rng_state, internal.n_EE[i - 1 + (shared.k_E - 1) * shared.dim.n_EE.mult[1] + (j - 1) * shared.dim.n_EE.mult[2]], 1 - shared.p_C[i - 1]);
      }
    }
    for (size_t i = 1; i <= shared.dim.n_sympt_to_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.n_sympt_to_hosp.dim[1]; ++j) {
        internal.n_sympt_to_hosp[i - 1 + (j - 1) * shared.dim.n_sympt_to_hosp.mult[1]] = monty::random::binomial<real_type>(rng_state, internal.n_II_C[i - 1 + (shared.k_C - 1) * shared.dim.n_II_C.mult[1] + (j - 1) * shared.dim.n_II_C.mult[2]], 1 - shared.p_recov_sympt[i - 1]);
      }
    }
    for (size_t i = 1; i <= shared.dim.n_death_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.n_death_hosp.dim[1]; ++j) {
        internal.n_death_hosp[i - 1 + (j - 1) * shared.dim.n_death_hosp.mult[1]] = monty::random::binomial<real_type>(rng_state, internal.n_II_hosp[i - 1 + (shared.k_hosp - 1) * shared.dim.n_II_hosp.mult[1] + (j - 1) * shared.dim.n_II_hosp.mult[2]], shared.p_death_hosp[i - 1]);
      }
    }
    for (size_t i = 1; i <= shared.dim.n_ICU_to_R_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.n_ICU_to_R_hosp.dim[1]; ++j) {
        internal.n_ICU_to_R_hosp[i - 1 + (j - 1) * shared.dim.n_ICU_to_R_hosp.mult[1]] = monty::random::binomial<real_type>(rng_state, internal.n_II_ICU[i - 1 + (shared.k_ICU - 1) * shared.dim.n_II_ICU.mult[1] + (j - 1) * shared.dim.n_II_ICU.mult[2]], shared.p_recov_ICU[i - 1]);
      }
    }
    for (size_t i = 1; i <= shared.dim.I_with_diff_trans.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.I_with_diff_trans.dim[1]; ++j) {
        internal.I_with_diff_trans[i - 1 + (j - 1) * shared.dim.I_with_diff_trans.mult[1]] = shared.trans_increase[i - 1 + (j - 1) * shared.dim.trans_increase.mult[1]] * (dust2::array::sum<real_type>(I_A, shared.dim.I_A, {i - 1, i - 1}, {0, shared.dim.I_A.dim[1] - 1}, {j - 1, j - 1}) + dust2::array::sum<real_type>(I_C, shared.dim.I_C, {i - 1, i - 1}, {0, shared.dim.I_C.dim[1] - 1}, {j - 1, j - 1}) + shared.hosp_transmission * dust2::array::sum<real_type>(I_hosp, shared.dim.I_hosp, {i - 1, i - 1}, {0, shared.dim.I_hosp.dim[1] - 1}, {j - 1, j - 1}) + shared.ICU_transmission * dust2::array::sum<real_type>(I_ICU, shared.dim.I_ICU, {i - 1, i - 1}, {0, shared.dim.I_ICU.dim[1] - 1}, {j - 1, j - 1}));
      }
    }
    const real_type seed = shared.interpolate_seed.eval(time);
    const real_type beta = shared.interpolate_beta.eval(time);
    for (size_t i = 1; i <= shared.dim.n_EI_C.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.n_EI_C.dim[1]; ++j) {
        internal.n_EI_C[i - 1 + (j - 1) * shared.dim.n_EI_C.mult[1]] = internal.n_EE[i - 1 + (shared.k_E - 1) * shared.dim.n_EE.mult[1] + (j - 1) * shared.dim.n_EE.mult[2]] - internal.n_EI_A[i - 1 + (j - 1) * shared.dim.n_EI_A.mult[1]];
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_II_A.dim[0]; ++i) {
      for (size_t k = 1; k <= shared.dim.aux_II_A.dim[2]; ++k) {
        internal.aux_II_A[i - 1 + (k - 1) * shared.dim.aux_II_A.mult[2]] = internal.n_EI_A[i - 1 + (k - 1) * shared.dim.n_EI_A.mult[1]];
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_II_A.dim[0]; ++i) {
      for (size_t j = 2; j <= static_cast<size_t>(shared.k_A); ++j) {
        for (size_t k = 1; k <= shared.dim.aux_II_A.dim[2]; ++k) {
          internal.aux_II_A[i - 1 + (j - 1) * shared.dim.aux_II_A.mult[1] + (k - 1) * shared.dim.aux_II_A.mult[2]] = internal.n_II_A[i - 1 + (j - 1 - 1) * shared.dim.n_II_A.mult[1] + (k - 1) * shared.dim.n_II_A.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_II_A.dim[0]; ++i) {
      for (size_t j = 1; j <= static_cast<size_t>(shared.k_A); ++j) {
        for (size_t k = 1; k <= shared.dim.aux_II_A.dim[2]; ++k) {
          internal.aux_II_A[i - 1 + (j - 1) * shared.dim.aux_II_A.mult[1] + (k - 1) * shared.dim.aux_II_A.mult[2]] = internal.aux_II_A[i - 1 + (j - 1) * shared.dim.aux_II_A.mult[1] + (k - 1) * shared.dim.aux_II_A.mult[2]] - internal.n_II_A[i - 1 + (j - 1) * shared.dim.n_II_A.mult[1] + (k - 1) * shared.dim.n_II_A.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_II_hosp.dim[0]; ++i) {
      for (size_t k = 1; k <= shared.dim.aux_II_hosp.dim[2]; ++k) {
        internal.aux_II_hosp[i - 1 + (k - 1) * shared.dim.aux_II_hosp.mult[2]] = internal.n_sympt_to_hosp[i - 1 + (k - 1) * shared.dim.n_sympt_to_hosp.mult[1]];
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_II_hosp.dim[0]; ++i) {
      for (size_t j = 2; j <= static_cast<size_t>(shared.k_hosp); ++j) {
        for (size_t k = 1; k <= shared.dim.aux_II_hosp.dim[2]; ++k) {
          internal.aux_II_hosp[i - 1 + (j - 1) * shared.dim.aux_II_hosp.mult[1] + (k - 1) * shared.dim.aux_II_hosp.mult[2]] = internal.n_II_hosp[i - 1 + (j - 1 - 1) * shared.dim.n_II_hosp.mult[1] + (k - 1) * shared.dim.n_II_hosp.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_II_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= static_cast<size_t>(shared.k_hosp); ++j) {
        for (size_t k = 1; k <= shared.dim.aux_II_hosp.dim[2]; ++k) {
          internal.aux_II_hosp[i - 1 + (j - 1) * shared.dim.aux_II_hosp.mult[1] + (k - 1) * shared.dim.aux_II_hosp.mult[2]] = internal.aux_II_hosp[i - 1 + (j - 1) * shared.dim.aux_II_hosp.mult[1] + (k - 1) * shared.dim.aux_II_hosp.mult[2]] - internal.n_II_hosp[i - 1 + (j - 1) * shared.dim.n_II_hosp.mult[1] + (k - 1) * shared.dim.n_II_hosp.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.n_hosp_to_ICU.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.n_hosp_to_ICU.dim[1]; ++j) {
        internal.n_hosp_to_ICU[i - 1 + (j - 1) * shared.dim.n_hosp_to_ICU.mult[1]] = monty::random::binomial<real_type>(rng_state, internal.n_II_hosp[i - 1 + (shared.k_hosp - 1) * shared.dim.n_II_hosp.mult[1] + (j - 1) * shared.dim.n_II_hosp.mult[2]] - internal.n_death_hosp[i - 1 + (j - 1) * shared.dim.n_death_hosp.mult[1]], 1 - shared.p_recov_hosp[i - 1] - shared.p_death_hosp[i - 1]);
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_R_hosp.dim[0]; ++i) {
      for (size_t k = 1; k <= shared.dim.aux_R_hosp.dim[2]; ++k) {
        internal.aux_R_hosp[i - 1 + (k - 1) * shared.dim.aux_R_hosp.mult[2]] = internal.n_ICU_to_R_hosp[i - 1 + (k - 1) * shared.dim.n_ICU_to_R_hosp.mult[1]];
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_R_hosp.dim[0]; ++i) {
      for (size_t j = 2; j <= static_cast<size_t>(shared.k_rec); ++j) {
        for (size_t k = 1; k <= shared.dim.aux_R_hosp.dim[2]; ++k) {
          internal.aux_R_hosp[i - 1 + (j - 1) * shared.dim.aux_R_hosp.mult[1] + (k - 1) * shared.dim.aux_R_hosp.mult[2]] = internal.n_R_hosp[i - 1 + (j - 1 - 1) * shared.dim.n_R_hosp.mult[1] + (k - 1) * shared.dim.n_R_hosp.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_R_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= static_cast<size_t>(shared.k_rec); ++j) {
        for (size_t k = 1; k <= shared.dim.aux_R_hosp.dim[2]; ++k) {
          internal.aux_R_hosp[i - 1 + (j - 1) * shared.dim.aux_R_hosp.mult[1] + (k - 1) * shared.dim.aux_R_hosp.mult[2]] = internal.aux_R_hosp[i - 1 + (j - 1) * shared.dim.aux_R_hosp.mult[1] + (k - 1) * shared.dim.aux_R_hosp.mult[2]] - internal.n_R_hosp[i - 1 + (j - 1) * shared.dim.n_R_hosp.mult[1] + (k - 1) * shared.dim.n_R_hosp.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.delta_D.size; ++i) {
      internal.delta_D[i - 1] = dust2::array::sum<real_type>(internal.n_II_ICU.data(), shared.dim.n_II_ICU, {i - 1, i - 1}, {shared.k_ICU - 1, shared.k_ICU - 1}, {0, shared.dim.n_II_ICU.dim[2] - 1}) - dust2::array::sum<real_type>(internal.n_ICU_to_R_hosp.data(), shared.dim.n_ICU_to_R_hosp, {i - 1, i - 1}, {0, shared.dim.n_ICU_to_R_hosp.dim[1] - 1}) + dust2::array::sum<real_type>(internal.n_death_hosp.data(), shared.dim.n_death_hosp, {i - 1, i - 1}, {0, shared.dim.n_death_hosp.dim[1] - 1});
    }
    for (size_t i = 1; i <= shared.dim.s_ij.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.s_ij.dim[1]; ++j) {
        internal.s_ij[i - 1 + (j - 1) * shared.dim.s_ij.mult[1]] = shared.m[i - 1 + (j - 1) * shared.dim.m.mult[1]] * dust2::array::sum<real_type>(internal.I_with_diff_trans.data(), shared.dim.I_with_diff_trans, {j - 1, j - 1}, {0, shared.dim.I_with_diff_trans.dim[1] - 1});
      }
    }
    for (size_t i = 1; i <= shared.dim.delta_I_A.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.delta_I_A.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.delta_I_A.dim[2]; ++k) {
          internal.delta_I_A[i - 1 + (j - 1) * shared.dim.delta_I_A.mult[1] + (k - 1) * shared.dim.delta_I_A.mult[2]] = internal.aux_II_A[i - 1 + (j - 1) * shared.dim.aux_II_A.mult[1] + (k - 1) * shared.dim.aux_II_A.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_II_C.dim[0]; ++i) {
      for (size_t k = 1; k <= shared.dim.aux_II_C.dim[2]; ++k) {
        internal.aux_II_C[i - 1 + (k - 1) * shared.dim.aux_II_C.mult[2]] = internal.n_EI_C[i - 1 + (k - 1) * shared.dim.n_EI_C.mult[1]];
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_II_C.dim[0]; ++i) {
      for (size_t j = 2; j <= static_cast<size_t>(shared.k_C); ++j) {
        for (size_t k = 1; k <= shared.dim.aux_II_C.dim[2]; ++k) {
          internal.aux_II_C[i - 1 + (j - 1) * shared.dim.aux_II_C.mult[1] + (k - 1) * shared.dim.aux_II_C.mult[2]] = internal.n_II_C[i - 1 + (j - 1 - 1) * shared.dim.n_II_C.mult[1] + (k - 1) * shared.dim.n_II_C.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_II_C.dim[0]; ++i) {
      for (size_t j = 1; j <= static_cast<size_t>(shared.k_C); ++j) {
        for (size_t k = 1; k <= shared.dim.aux_II_C.dim[2]; ++k) {
          internal.aux_II_C[i - 1 + (j - 1) * shared.dim.aux_II_C.mult[1] + (k - 1) * shared.dim.aux_II_C.mult[2]] = internal.aux_II_C[i - 1 + (j - 1) * shared.dim.aux_II_C.mult[1] + (k - 1) * shared.dim.aux_II_C.mult[2]] - internal.n_II_C[i - 1 + (j - 1) * shared.dim.n_II_C.mult[1] + (k - 1) * shared.dim.n_II_C.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.delta_I_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.delta_I_hosp.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.delta_I_hosp.dim[2]; ++k) {
          internal.delta_I_hosp[i - 1 + (j - 1) * shared.dim.delta_I_hosp.mult[1] + (k - 1) * shared.dim.delta_I_hosp.mult[2]] = internal.aux_II_hosp[i - 1 + (j - 1) * shared.dim.aux_II_hosp.mult[1] + (k - 1) * shared.dim.aux_II_hosp.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_II_ICU.dim[0]; ++i) {
      for (size_t k = 1; k <= shared.dim.aux_II_ICU.dim[2]; ++k) {
        internal.aux_II_ICU[i - 1 + (k - 1) * shared.dim.aux_II_ICU.mult[2]] = internal.n_hosp_to_ICU[i - 1 + (k - 1) * shared.dim.n_hosp_to_ICU.mult[1]];
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_II_ICU.dim[0]; ++i) {
      for (size_t j = 2; j <= static_cast<size_t>(shared.k_ICU); ++j) {
        for (size_t k = 1; k <= shared.dim.aux_II_ICU.dim[2]; ++k) {
          internal.aux_II_ICU[i - 1 + (j - 1) * shared.dim.aux_II_ICU.mult[1] + (k - 1) * shared.dim.aux_II_ICU.mult[2]] = internal.n_II_ICU[i - 1 + (j - 1 - 1) * shared.dim.n_II_ICU.mult[1] + (k - 1) * shared.dim.n_II_ICU.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_II_ICU.dim[0]; ++i) {
      for (size_t j = 1; j <= static_cast<size_t>(shared.k_ICU); ++j) {
        for (size_t k = 1; k <= shared.dim.aux_II_ICU.dim[2]; ++k) {
          internal.aux_II_ICU[i - 1 + (j - 1) * shared.dim.aux_II_ICU.mult[1] + (k - 1) * shared.dim.aux_II_ICU.mult[2]] = internal.aux_II_ICU[i - 1 + (j - 1) * shared.dim.aux_II_ICU.mult[1] + (k - 1) * shared.dim.aux_II_ICU.mult[2]] - internal.n_II_ICU[i - 1 + (j - 1) * shared.dim.n_II_ICU.mult[1] + (k - 1) * shared.dim.n_II_ICU.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.delta_R_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.delta_R_hosp.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.delta_R_hosp.dim[2]; ++k) {
          internal.delta_R_hosp[i - 1 + (j - 1) * shared.dim.delta_R_hosp.mult[1] + (k - 1) * shared.dim.delta_R_hosp.mult[2]] = internal.aux_R_hosp[i - 1 + (j - 1) * shared.dim.aux_R_hosp.mult[1] + (k - 1) * shared.dim.aux_R_hosp.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.new_D.size; ++i) {
      internal.new_D[i - 1] = internal.delta_D[i - 1];
    }
    for (size_t i = 1; i <= shared.dim.delta_R.size; ++i) {
      internal.delta_R[i - 1] = dust2::array::sum<real_type>(internal.n_II_A.data(), shared.dim.n_II_A, {i - 1, i - 1}, {shared.k_A - 1, shared.k_A - 1}, {0, shared.dim.n_II_A.dim[2] - 1}) + dust2::array::sum<real_type>(internal.n_II_C.data(), shared.dim.n_II_C, {i - 1, i - 1}, {shared.k_C - 1, shared.k_C - 1}, {0, shared.dim.n_II_C.dim[2] - 1}) - dust2::array::sum<real_type>(internal.n_sympt_to_hosp.data(), shared.dim.n_sympt_to_hosp, {i - 1, i - 1}, {0, shared.dim.n_sympt_to_hosp.dim[1] - 1}) + dust2::array::sum<real_type>(internal.n_II_hosp.data(), shared.dim.n_II_hosp, {i - 1, i - 1}, {shared.k_hosp - 1, shared.k_hosp - 1}, {0, shared.dim.n_II_hosp.dim[2] - 1}) - dust2::array::sum<real_type>(internal.n_hosp_to_ICU.data(), shared.dim.n_hosp_to_ICU, {i - 1, i - 1}, {0, shared.dim.n_hosp_to_ICU.dim[1] - 1}) - dust2::array::sum<real_type>(internal.n_death_hosp.data(), shared.dim.n_death_hosp, {i - 1, i - 1}, {0, shared.dim.n_death_hosp.dim[1] - 1}) + dust2::array::sum<real_type>(internal.n_R_hosp.data(), shared.dim.n_R_hosp, {i - 1, i - 1}, {shared.k_rec - 1, shared.k_rec - 1}, {0, shared.dim.n_R_hosp.dim[2] - 1});
    }
    for (size_t i = 1; i <= shared.dim.lambda.size; ++i) {
      internal.lambda[i - 1] = beta * dust2::array::sum<real_type>(internal.s_ij.data(), shared.dim.s_ij, {i - 1, i - 1}, {0, shared.dim.s_ij.dim[1] - 1});
    }
    for (size_t i = 1; i <= shared.dim.p_SE.size; ++i) {
      internal.p_SE[i - 1] = 1 - monty::math::exp(-internal.lambda[i - 1] * dt);
    }
    for (size_t i = 1; i <= shared.dim.delta_I_C.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.delta_I_C.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.delta_I_C.dim[2]; ++k) {
          internal.delta_I_C[i - 1 + (j - 1) * shared.dim.delta_I_C.mult[1] + (k - 1) * shared.dim.delta_I_C.mult[2]] = internal.aux_II_C[i - 1 + (j - 1) * shared.dim.aux_II_C.mult[1] + (k - 1) * shared.dim.aux_II_C.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.delta_I_ICU.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.delta_I_ICU.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.delta_I_ICU.dim[2]; ++k) {
          internal.delta_I_ICU[i - 1 + (j - 1) * shared.dim.delta_I_ICU.mult[1] + (k - 1) * shared.dim.delta_I_ICU.mult[2]] = internal.aux_II_ICU[i - 1 + (j - 1) * shared.dim.aux_II_ICU.mult[1] + (k - 1) * shared.dim.aux_II_ICU.mult[2]];
        }
      }
    }
    const real_type tot_new_D = dust2::array::sum<real_type>(internal.new_D.data(), shared.dim.new_D);
    for (size_t i = 1; i <= shared.dim.n_SE.size; ++i) {
      internal.n_SE[i - 1] = monty::random::binomial<real_type>(rng_state, S[i - 1], internal.p_SE[i - 1]);
    }
    {
      const size_t i = shared.seed_age_band;
      internal.n_SE[shared.seed_age_band - 1] = monty::math::min<real_type>(S[i - 1], internal.n_SE[i - 1] + monty::random::poisson<real_type>(rng_state, seed));
    }
    for (size_t i = 1; i <= shared.dim.new_I_ICU.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.new_I_ICU.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.new_I_ICU.dim[2]; ++k) {
          internal.new_I_ICU[i - 1 + (j - 1) * shared.dim.new_I_ICU.mult[1] + (k - 1) * shared.dim.new_I_ICU.mult[2]] = I_ICU[i - 1 + (j - 1) * shared.dim.I_ICU.mult[1] + (k - 1) * shared.dim.I_ICU.mult[2]] + internal.delta_I_ICU[i - 1 + (j - 1) * shared.dim.delta_I_ICU.mult[1] + (k - 1) * shared.dim.delta_I_ICU.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_EE.dim[0]; ++i) {
      internal.aux_EE[i - 1] = monty::random::binomial<real_type>(rng_state, internal.n_SE[i - 1], shared.aux_p_bin[i - 1]);
    }
    for (size_t i = 1; i <= shared.dim.aux_EE.dim[0]; ++i) {
      for (size_t k = 2; k <= static_cast<size_t>((shared.n_trans_classes - 1)); ++k) {
        internal.aux_EE[i - 1 + (k - 1) * shared.dim.aux_EE.mult[2]] = monty::random::binomial<real_type>(rng_state, internal.n_SE[i - 1] - dust2::array::sum<real_type>(internal.aux_EE.data(), shared.dim.aux_EE, {i - 1, i - 1}, {0, 0}, {0, (k - 1) - 1}), shared.aux_p_bin[i - 1 + (k - 1) * shared.dim.aux_p_bin.mult[1]]);
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_EE.dim[0]; ++i) {
      internal.aux_EE[i - 1 + (shared.n_trans_classes - 1) * shared.dim.aux_EE.mult[2]] = internal.n_SE[i - 1] - dust2::array::sum<real_type>(internal.aux_EE.data(), shared.dim.aux_EE, {i - 1, i - 1}, {0, 0}, {0, (shared.n_trans_classes - 1) - 1});
    }
    for (size_t i = 1; i <= shared.dim.aux_EE.dim[0]; ++i) {
      for (size_t j = 2; j <= static_cast<size_t>(shared.k_E); ++j) {
        for (size_t k = 1; k <= shared.dim.aux_EE.dim[2]; ++k) {
          internal.aux_EE[i - 1 + (j - 1) * shared.dim.aux_EE.mult[1] + (k - 1) * shared.dim.aux_EE.mult[2]] = internal.n_EE[i - 1 + (j - 1 - 1) * shared.dim.n_EE.mult[1] + (k - 1) * shared.dim.n_EE.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.aux_EE.dim[0]; ++i) {
      for (size_t j = 1; j <= static_cast<size_t>(shared.k_E); ++j) {
        for (size_t k = 1; k <= shared.dim.aux_EE.dim[2]; ++k) {
          internal.aux_EE[i - 1 + (j - 1) * shared.dim.aux_EE.mult[1] + (k - 1) * shared.dim.aux_EE.mult[2]] = internal.aux_EE[i - 1 + (j - 1) * shared.dim.aux_EE.mult[1] + (k - 1) * shared.dim.aux_EE.mult[2]] - internal.n_EE[i - 1 + (j - 1) * shared.dim.n_EE.mult[1] + (k - 1) * shared.dim.n_EE.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.delta_E.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.delta_E.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.delta_E.dim[2]; ++k) {
          internal.delta_E[i - 1 + (j - 1) * shared.dim.delta_E.mult[1] + (k - 1) * shared.dim.delta_E.mult[2]] = internal.aux_EE[i - 1 + (j - 1) * shared.dim.aux_EE.mult[1] + (k - 1) * shared.dim.aux_EE.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.S.size; ++i) {
      state_next[i - 1 + 5] = S[i - 1] - internal.n_SE[i - 1];
    }
    for (size_t i = 1; i <= shared.dim.E.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.E.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.E.dim[2]; ++k) {
          state_next[i - 1 + (j - 1) * shared.dim.E.mult[1] + (k - 1) * shared.dim.E.mult[2] + shared.odin.offset.state[6]] = E[i - 1 + (j - 1) * shared.dim.E.mult[1] + (k - 1) * shared.dim.E.mult[2]] + internal.delta_E[i - 1 + (j - 1) * shared.dim.delta_E.mult[1] + (k - 1) * shared.dim.delta_E.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.I_A.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.I_A.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.I_A.dim[2]; ++k) {
          state_next[i - 1 + (j - 1) * shared.dim.I_A.mult[1] + (k - 1) * shared.dim.I_A.mult[2] + shared.odin.offset.state[7]] = I_A[i - 1 + (j - 1) * shared.dim.I_A.mult[1] + (k - 1) * shared.dim.I_A.mult[2]] + internal.delta_I_A[i - 1 + (j - 1) * shared.dim.delta_I_A.mult[1] + (k - 1) * shared.dim.delta_I_A.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.I_C.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.I_C.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.I_C.dim[2]; ++k) {
          state_next[i - 1 + (j - 1) * shared.dim.I_C.mult[1] + (k - 1) * shared.dim.I_C.mult[2] + shared.odin.offset.state[8]] = I_C[i - 1 + (j - 1) * shared.dim.I_C.mult[1] + (k - 1) * shared.dim.I_C.mult[2]] + internal.delta_I_C[i - 1 + (j - 1) * shared.dim.delta_I_C.mult[1] + (k - 1) * shared.dim.delta_I_C.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.R.size; ++i) {
      state_next[i - 1 + shared.odin.offset.state[12]] = R[i - 1] + internal.delta_R[i - 1];
    }
    for (size_t i = 1; i <= shared.dim.I_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.I_hosp.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.I_hosp.dim[2]; ++k) {
          state_next[i - 1 + (j - 1) * shared.dim.I_hosp.mult[1] + (k - 1) * shared.dim.I_hosp.mult[2] + shared.odin.offset.state[9]] = I_hosp[i - 1 + (j - 1) * shared.dim.I_hosp.mult[1] + (k - 1) * shared.dim.I_hosp.mult[2]] + internal.delta_I_hosp[i - 1 + (j - 1) * shared.dim.delta_I_hosp.mult[1] + (k - 1) * shared.dim.delta_I_hosp.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.I_ICU.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.I_ICU.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.I_ICU.dim[2]; ++k) {
          state_next[i - 1 + (j - 1) * shared.dim.I_ICU.mult[1] + (k - 1) * shared.dim.I_ICU.mult[2] + shared.odin.offset.state[10]] = internal.new_I_ICU[i - 1 + (j - 1) * shared.dim.new_I_ICU.mult[1] + (k - 1) * shared.dim.new_I_ICU.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.R_hosp.dim[0]; ++i) {
      for (size_t j = 1; j <= shared.dim.R_hosp.dim[1]; ++j) {
        for (size_t k = 1; k <= shared.dim.R_hosp.dim[2]; ++k) {
          state_next[i - 1 + (j - 1) * shared.dim.R_hosp.mult[1] + (k - 1) * shared.dim.R_hosp.mult[2] + shared.odin.offset.state[11]] = R_hosp[i - 1 + (j - 1) * shared.dim.R_hosp.mult[1] + (k - 1) * shared.dim.R_hosp.mult[2]] + internal.delta_R_hosp[i - 1 + (j - 1) * shared.dim.delta_R_hosp.mult[1] + (k - 1) * shared.dim.delta_R_hosp.mult[2]];
        }
      }
    }
    for (size_t i = 1; i <= shared.dim.D.size; ++i) {
      state_next[i - 1 + shared.odin.offset.state[13]] = D[i - 1] + internal.new_D[i - 1];
    }
    state_next[1] = dust2::array::sum<real_type>(internal.new_I_ICU.data(), shared.dim.new_I_ICU);
    state_next[2] = D_tot + tot_new_D;
    state_next[3] = D_inc + tot_new_D;
    state_next[4] = beta;
    state_next[0] = dust2::array::sum<real_type>(S, shared.dim.S) + dust2::array::sum<real_type>(R, shared.dim.R) + dust2::array::sum<real_type>(D, shared.dim.D) + dust2::array::sum<real_type>(E, shared.dim.E) + dust2::array::sum<real_type>(I_A, shared.dim.I_A) + dust2::array::sum<real_type>(I_C, shared.dim.I_C) + dust2::array::sum<real_type>(I_hosp, shared.dim.I_hosp) + dust2::array::sum<real_type>(I_ICU, shared.dim.I_ICU) + dust2::array::sum<real_type>(R_hosp, shared.dim.R_hosp);
  }
  static auto zero_every(const shared_state& shared) {
    return dust2::zero_every_type<real_type>{{1, {3}}};
  }
  static real_type compare_data(real_type time, const real_type* state, const data_type& data, const shared_state& shared, internal_state& internal, rng_state_type& rng_state) {
    const auto I_ICU_tot = state[1];
    const auto D_inc = state[3];
    real_type odin_ll = 0;
    const real_type icu_with_noise = shared.phi_ICU * I_ICU_tot + monty::random::exponential_rate<real_type>(rng_state, shared.exp_noise);
    const real_type deaths_with_noise = shared.phi_death * D_inc + monty::random::exponential_rate<real_type>(rng_state, shared.exp_noise);
    if (!std::isnan(data.icu)) {
      odin_ll += monty::density::negative_binomial_mu(data.icu, shared.kappa_ICU, icu_with_noise, true);
    }
    if (!std::isnan(data.deaths)) {
      odin_ll += monty::density::negative_binomial_mu(data.deaths, shared.kappa_death, deaths_with_noise, true);
    }
    return odin_ll;
  }
};

#include <cpp11.hpp>
#include <dust2/r/discrete/system.hpp>
#include <dust2/r/discrete/filter.hpp>
#include <dust2/r/discrete/unfilter.hpp>

[[cpp11::register]]
SEXP dust2_system_basic_alloc(cpp11::list r_pars, cpp11::sexp r_time, cpp11::list r_time_control, cpp11::sexp r_n_particles, cpp11::sexp r_n_groups, cpp11::sexp r_seed, cpp11::sexp r_deterministic, cpp11::sexp r_n_threads) {
  return dust2::r::dust2_discrete_alloc<basic>(r_pars, r_time, r_time_control, r_n_particles, r_n_groups, r_seed, r_deterministic, r_n_threads);
}
[[cpp11::register]]
SEXP dust2_system_basic_run_to_time(cpp11::sexp ptr, cpp11::sexp r_time) {
  return dust2::r::dust2_system_run_to_time<dust2::dust_discrete<basic>>(ptr, r_time);
}

[[cpp11::register]]
SEXP dust2_system_basic_state(cpp11::sexp ptr, cpp11::sexp r_index_state, cpp11::sexp r_index_particle, cpp11::sexp r_index_group, bool preserve_particle_dimension, bool preserve_group_dimension) {
  return dust2::r::dust2_system_state<dust2::dust_discrete<basic>>(ptr, r_index_state, r_index_particle, r_index_group, preserve_particle_dimension, preserve_group_dimension);
}

[[cpp11::register]]
SEXP dust2_system_basic_time(cpp11::sexp ptr) {
  return dust2::r::dust2_system_time<dust2::dust_discrete<basic>>(ptr);
}

[[cpp11::register]]
SEXP dust2_system_basic_set_state_initial(cpp11::sexp ptr) {
  return dust2::r::dust2_system_set_state_initial<dust2::dust_discrete<basic>>(ptr);
}

[[cpp11::register]]
SEXP dust2_system_basic_set_state(cpp11::sexp ptr, cpp11::list r_state) {
  return dust2::r::dust2_system_set_state<dust2::dust_discrete<basic>>(ptr, r_state);
}

[[cpp11::register]]
SEXP dust2_system_basic_reorder(cpp11::sexp ptr, cpp11::integers r_index) {
  return dust2::r::dust2_system_reorder<dust2::dust_discrete<basic>>(ptr, r_index);
}

[[cpp11::register]]
SEXP dust2_system_basic_rng_state(cpp11::sexp ptr) {
  return dust2::r::dust2_system_rng_state<dust2::dust_discrete<basic>>(ptr);
}

[[cpp11::register]]
SEXP dust2_system_basic_set_rng_state(cpp11::sexp ptr, cpp11::sexp r_rng_state) {
  return dust2::r::dust2_system_set_rng_state<dust2::dust_discrete<basic>>(ptr, r_rng_state);
}

[[cpp11::register]]
SEXP dust2_system_basic_set_time(cpp11::sexp ptr, cpp11::sexp r_time) {
  return dust2::r::dust2_system_set_time<dust2::dust_discrete<basic>>(ptr, r_time);
}

[[cpp11::register]]
SEXP dust2_system_basic_update_pars(cpp11::sexp ptr, cpp11::list pars) {
  return dust2::r::dust2_system_update_pars<dust2::dust_discrete<basic>>(ptr, pars);
}

[[cpp11::register]]
SEXP dust2_system_basic_simulate(cpp11::sexp ptr, cpp11::sexp r_times, cpp11::sexp r_index_state, bool preserve_particle_dimension, bool preserve_group_dimension) {
  return dust2::r::dust2_system_simulate<dust2::dust_discrete<basic>>(ptr, r_times, r_index_state, preserve_particle_dimension, preserve_group_dimension);
}

[[cpp11::register]]
SEXP dust2_unfilter_basic_alloc(cpp11::list r_pars, cpp11::sexp r_time_start, cpp11::sexp r_time, cpp11::list r_time_control, cpp11::list r_data, cpp11::sexp r_n_particles, cpp11::sexp r_n_groups, cpp11::sexp r_n_threads) {
  return dust2::r::dust2_discrete_unfilter_alloc<basic>(r_pars, r_time_start, r_time, r_time_control, r_data, r_n_particles, r_n_groups, r_n_threads);
}

[[cpp11::register]]
SEXP dust2_filter_basic_alloc(cpp11::list r_pars, cpp11::sexp r_time_start, cpp11::sexp r_time, cpp11::list r_time_control, cpp11::list r_data, cpp11::sexp r_n_particles, cpp11::sexp r_n_groups, cpp11::sexp r_n_threads, cpp11::sexp r_seed) {
  return dust2::r::dust2_discrete_filter_alloc<basic>(r_pars, r_time_start, r_time, r_time_control, r_data, r_n_particles, r_n_groups, r_n_threads, r_seed);
}
[[cpp11::register]]
SEXP dust2_system_basic_compare_data(cpp11::sexp ptr, cpp11::list r_data, bool preserve_particle_dimension, bool preserve_group_dimension) {
  return dust2::r::dust2_system_compare_data<dust2::dust_discrete<basic>>(ptr, r_data, preserve_particle_dimension, preserve_group_dimension);
}

[[cpp11::register]]
SEXP dust2_unfilter_basic_update_pars(cpp11::sexp ptr, cpp11::list r_pars, cpp11::sexp r_index_group) {
  return dust2::r::dust2_unfilter_update_pars<dust2::dust_discrete<basic>>(ptr, r_pars, r_index_group);
}

[[cpp11::register]]
SEXP dust2_unfilter_basic_run(cpp11::sexp ptr, cpp11::sexp r_initial, bool save_trajectories, cpp11::sexp save_snapshots, bool adjoint, cpp11::sexp r_index_state, cpp11::sexp r_index_group, bool preserve_particle_dimension, bool preserve_group_dimension) {
  return dust2::r::dust2_unfilter_run<dust2::dust_discrete<basic>>(ptr, r_initial, save_trajectories, save_snapshots, adjoint, r_index_state, r_index_group, preserve_particle_dimension, preserve_group_dimension);
}

[[cpp11::register]]
SEXP dust2_unfilter_basic_last_trajectories(cpp11::sexp ptr, bool select_random_particle, bool preserve_particle_dimension, bool preserve_group_dimension) {
  return dust2::r::dust2_unfilter_last_trajectories<dust2::dust_discrete<basic>>(ptr, select_random_particle, preserve_particle_dimension, preserve_group_dimension);
}

[[cpp11::register]]
SEXP dust2_unfilter_basic_last_snapshots(cpp11::sexp ptr, bool select_random_particle, bool preserve_particle_dimension, bool preserve_group_dimension) {
  return dust2::r::dust2_unfilter_last_snapshots<dust2::dust_discrete<basic>>(ptr, select_random_particle, preserve_particle_dimension, preserve_group_dimension);
}

[[cpp11::register]]
SEXP dust2_unfilter_basic_last_state(cpp11::sexp ptr, bool select_random_particle, bool preserve_particle_dimension, bool preserve_group_dimension) {
  return dust2::r::dust2_unfilter_last_state<dust2::dust_discrete<basic>>(ptr, select_random_particle, preserve_particle_dimension, preserve_group_dimension);
}

[[cpp11::register]]
SEXP dust2_filter_basic_update_pars(cpp11::sexp ptr, cpp11::list r_pars, cpp11::sexp r_index_group) {
  return dust2::r::dust2_filter_update_pars<dust2::dust_discrete<basic>>(ptr, r_pars, r_index_group);
}

[[cpp11::register]]
SEXP dust2_filter_basic_run(cpp11::sexp ptr, cpp11::sexp r_initial, bool save_trajectories, cpp11::sexp save_snapshots, bool adjoint, cpp11::sexp index_state, cpp11::sexp index_group, bool preserve_particle_dimension, bool preserve_group_dimension) {
  return dust2::r::dust2_filter_run<dust2::dust_discrete<basic>>(ptr, r_initial, save_trajectories, save_snapshots, adjoint, index_state, index_group, preserve_particle_dimension, preserve_group_dimension);
}

[[cpp11::register]]
SEXP dust2_filter_basic_last_trajectories(cpp11::sexp ptr, bool select_random_particle, bool preserve_particle_dimension, bool preserve_group_dimension) {
  return dust2::r::dust2_filter_last_trajectories<dust2::dust_discrete<basic>>(ptr, select_random_particle, preserve_particle_dimension, preserve_group_dimension);
}

[[cpp11::register]]
SEXP dust2_filter_basic_last_snapshots(cpp11::sexp ptr, bool select_random_particle, bool preserve_particle_dimension, bool preserve_group_dimension) {
  return dust2::r::dust2_filter_last_snapshots<dust2::dust_discrete<basic>>(ptr, select_random_particle, preserve_particle_dimension, preserve_group_dimension);
}

[[cpp11::register]]
SEXP dust2_filter_basic_last_state(cpp11::sexp ptr, bool select_random_particle, bool preserve_particle_dimension, bool preserve_group_dimension) {
  return dust2::r::dust2_filter_last_state<dust2::dust_discrete<basic>>(ptr, select_random_particle, preserve_particle_dimension, preserve_group_dimension);
}

[[cpp11::register]]
SEXP dust2_filter_basic_rng_state(cpp11::sexp ptr) {
  return dust2::r::dust2_filter_rng_state<dust2::dust_discrete<basic>>(ptr);
}

[[cpp11::register]]
SEXP dust2_filter_basic_set_rng_state(cpp11::sexp ptr, cpp11::sexp r_rng_state) {
  return dust2::r::dust2_filter_set_rng_state<dust2::dust_discrete<basic>>(ptr, r_rng_state);
}
