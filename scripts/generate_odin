#!/usr/bin/env Rscript
if (packageVersion("odin.dust") < "0.1.4") {
  stop("Please upgrade odin.dust to at least 0.1.4")
}
withr::with_options(
  list(odin.no_check_naked_index = TRUE),
  odin.dust::odin_dust_package(here::here()))
